import { Helmet } from "react-helmet";

const Reservations = () => {
  return (
    <div>
      <Helmet>
        <title>Reservation</title>
      </Helmet>
    </div>
  );
}

export default Reservations
